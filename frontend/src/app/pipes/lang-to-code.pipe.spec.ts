import { LangToCodePipe } from './lang-to-code.pipe';

describe('LangToCodePipe', () => {
  it('create an instance', () => {
    const pipe = new LangToCodePipe();
    expect(pipe).toBeTruthy();
  });
});
